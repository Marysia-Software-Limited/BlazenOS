# 17 — Mobile monorepo

`blazen_os` is a **monorepo** that hosts three product surfaces — the
Raspberry Pi 5 appliance, the native Android app, and the native iOS
app — plus the shared Rust core that ties them together.

This document is the map.

## 1. The three surfaces

| Surface       | Lives in                                       | Stack                                                     | Status |
|---------------|------------------------------------------------|------------------------------------------------------------|--------|
| Pi 5 appliance| [`rpi5/src/blazend/`](../rpi5/src/blazend/), [`rpi5/<domain>/blazend-*`](../rpi5/), [`scripts/`](../scripts/), [`rpi5/stage-blazen/`](../rpi5/stage-blazen/) | Python + Rust, Raspberry Pi OS Lite, voice-first, no GUI    | M0 scaffolding |
| Android app   | [`android/`](../android/)                      | Kotlin 2.0 + Jetpack Compose + AGP 8.7, minSdk 30           | M0 scaffolding |
| iOS app       | [`ios/`](../ios/)                              | Swift 6.0 + SwiftUI + XcodeGen, iOS 17+                     | M0 scaffolding |

Each surface owns its own `README.md`, `AGENTS.md`, `CLAUDE.md`, and
`Makefile`. The monorepo root never overrides per-surface behaviour —
it provides the **shared core** and the **shared docs**.

> **New: a fourth node — [`macos/`](../macos/) (rachel).** Not a phone app but a
> full Jessica **host** on Apple Silicon that uses MLX/Metal for on-device LLM and
> joins a **shared LLM mesh + context** with the Pi (`jessica`) and Linux (`paul`)
> nodes via DSPy + `blazend-fabric`. See [`macos/docs/00-CONTEXT.md`](../macos/docs/00-CONTEXT.md)
> and [`macos/docs/03-LLM-MESH.md`](../macos/docs/03-LLM-MESH.md). Scaffold/design
> only so far — to be built by a separate Claude session on the Mac.

## 2. The shared core

```
domains/            # repo-root shared-core workspace — portable domain libraries
├── blazend-ipc/    # IPC wire / event envelope — contract
├── blazend-fabric/ # CRDT sync log — context
├── jessica-core/   # Pure Rust, no platform deps. — mind
│   ├── src/lib.rs         #   - SyncLog/Fact re-export, MemoryStore, RoutePlan, IntentRouter
│   ├── src/intent.rs      #   - Regex intent matcher + YAML parser
│   ├── src/context.rs     #   - Note/Reminder/MemoryStore (the memory model)
│   └── src/routing.rs     #   - Backend/RoutePlan (chat escalation policy)
└── jessica-ffi/           # C ABI + JNI bridge over jessica-core.
    ├── src/lib.rs         #   - extern "C" entry points (cbindgen-tracked)
    ├── src/jni_bridge.rs  #   - Java_os_blazen_jessica_core_* (Android)
    ├── include/jessica_ffi.h  # generated by cbindgen
    └── cbindgen.toml
```

Three artifacts ship to three consumers from the same Rust source:

| Artifact                       | Consumer                                                   |
|--------------------------------|------------------------------------------------------------|
| `JessicaFFI.xcframework`       | `ios/JessicaCore/` Swift Package (`binaryTarget(url:)`)    |
| `libjessica_ffi.so` (per ABI)  | `android/app/src/main/jniLibs/<abi>/` (loaded by `:core`)  |
| `rlib`                         | `cargo test --workspace` — keeps unit tests honest         |

The Pi 5 appliance does NOT consume `jessica-ffi` directly. The
appliance workspace at `rpi5/crates/Cargo.toml` path-depends on
`../../domains`, so the appliance uses the same `jessica-core` crate
for any shared business logic, and `blazend-fabric` for the sync log
on the wire.

> **Organized by capability domain (Phase 3 complete, 2026-06-29):** these
> shared crates are the **portable cores of their domains**, hoisted to the
> repo-root `domains/` workspace (flat; crate names unchanged). The intent/
> routing/memory types stay bundled in `jessica-core` for now — a per-domain
> split is deferred to Phase 4. The FFI contract and shipped artifacts are
> unchanged. See [`19-DOMAIN-ARCHITECTURE.md`](19-DOMAIN-ARCHITECTURE.md).

## 3. The contract

Three things are **identical** across all surfaces:

1. **`IntentMatch` JSON shape** —
   `name`, `language`, `action`, `tool?`, `params{}`, `confirm`.
   Defined in `jessica-core::intent`, mirrored in Kotlin
   `os.blazen.jessica.core.IntentMatch` and Swift `JessicaCore.IntentMatch`.
2. **YAML intent catalogue** —
   `configs/intents/system.yaml` is the canonical source. The Android
   and iOS bundles ship a snapshot under
   `android/app/src/main/assets/intents-system.yaml` and
   `ios/Jessica/Resources/intents-system.yaml`. The sync log carries
   user-added entries.
3. **Sync log facts** —
   `SyncLog::merge(Fact)` produces a `SyncMergeOutcome`. Same Rust
   crate, same byte-on-wire format, same conflict resolution.

When you change the contract:

- Update `jessica-core` first.
- Regenerate the cbindgen header (`cargo build -p jessica-ffi`).
- Update the JNI bridge if the signature changed.
- Update the Kotlin and Swift mirrors in the same commit.
- Update both bundled `intents-system.yaml` snapshots if the catalogue changed.

## 4. Who runs what on which host

| Action                                | paul (Linux) | macOS | Real Pi 5 | Mac + iPhone |
|---------------------------------------|:------------:|:-----:|:---------:|:------------:|
| `cargo test --workspace`              | ✓            | ✓     |           |              |
| `cd android && make build`            | ✓            | ✓     |           |              |
| `cd android && make install` (adb)    | ✓            | ✓     |           |              |
| `cd ios && make test` (swift test)    |              | ✓     |           | ✓            |
| `cd ios && make build` (xcodebuild)   |              | ✓     |           | ✓            |
| `make vm-image` (Pi 5 QEMU)           | ✓            |       |           |              |
| `make flash` (real SD card)           | ✓            |       | ✓ (target) |              |
| TestFlight build                      |              |       |           | ✓            |

paul is the **primary mobile dev rig** — it edits Swift / Kotlin / Rust
sources, runs `cargo`, drives `gradle` and `adb`, edits all docs. The
only thing paul can't do is the final iOS xcodebuild / TestFlight cut;
that step runs on the maintainer's Mac.

## 5. Per-surface entry points

| Surface  | Read these first                                                         |
|----------|--------------------------------------------------------------------------|
| Pi 5     | [`docs/01-ARCHITECTURE.md`](01-ARCHITECTURE.md), [`docs/04-VOICE-PIPELINE.md`](04-VOICE-PIPELINE.md), [`docs/12-ML-ACCELERATOR.md`](12-ML-ACCELERATOR.md) |
| Android  | [`/android/README.md`](../android/README.md), [`/android/docs/architecture.md`](../android/docs/architecture.md) |
| iOS      | [`/ios/README.md`](../ios/README.md), [`/ios/docs/architecture.md`](../ios/docs/architecture.md) |
| Shared   | [`docs/product/09-MOBILE-PLATFORM-DECISION.md`](product/09-MOBILE-PLATFORM-DECISION.md), [`docs/13-LANGUAGES.md`](13-LANGUAGES.md), [`docs/14-RUST-PYTHON-SPLIT.md`](14-RUST-PYTHON-SPLIT.md) |

## 6. Where the Pi 5 stack lives

- **Pi 5 appliance** is a self-contained project under `rpi5/` —
  `rpi5/src/blazend/` (Python), `rpi5/<domain>/blazend-*` (appliance Rust
  workspace at `rpi5/crates/Cargo.toml`), `rpi5/stage-blazen/`
  (pi-gen overlay), and `rpi5/tests/` (Tier 0-3 scenarios + fixtures).
- **`configs/`, `scripts/`, `docs/`** stay at the repo root — they're
  the cross-implementation contract that mobile reads too.
- **Two Cargo workspaces, one-directional:** `domains/Cargo.toml`
  (shared core — `jessica-core`, `jessica-ffi`, `blazend-ipc`,
  `blazend-fabric`) and `rpi5/crates/Cargo.toml` (appliance — members under
  `rpi5/<domain>/`, depends on the shared workspace via path). Mobile
  consumes only `domains/`.
- **`/docs/product/`** stays the shared cross-implementation spec.
  Updates here ripple to every surface.

Opening this repo on paul to do Pi 5 work runs the same `make` targets
as before; the only behavioural shift is that they now resolve into
`rpi5/`.

## 7. Sibling repos that are no longer authoritative

| Old location                          | Replaced by                          | Why                                |
|---------------------------------------|--------------------------------------|------------------------------------|
| `/Users/beret/dev/android/` (sibling) | [`/android/`](../android/) in repo   | Mobile work consolidates into the monorepo so Pi5 + mobile share core + docs + CI. |
| `/Users/beret/dev/ios/` (sibling)     | [`/ios/`](../ios/) in repo           | Same.                              |
| [`/Users/beret/dev/rachel/`](../../rachel/) | [`/ios/`](../ios/), [`/android/`](../android/) | Flutter prototype kept as a contract reference; the shipping apps are native. |
